# Minimal Python dependencies for Vercel serverless functions
# Only includes what the sync scripts actually need

# Core sync script dependencies
python-dotenv>=1.1.1
requests>=2.32.5
supabase>=2.18.1
